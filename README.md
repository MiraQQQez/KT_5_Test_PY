# 🧪 Тестирование API PetStore с Allure и Pydantic

## 📋 Описание проекта

Проект содержит автоматизированные тесты для API [PetStore Swagger](https://petstore.swagger.io/v2) с использованием:
- **Allure** - для красивых и информативных отчетов
- **Pydantic** - для валидации данных и автогенерации JSON
- **Pytest** - фреймворк для тестирования

## 🏗️ Структура проекта

```
KT_5_Test_PY/
├── api/
│   ├── __init__.py
│   ├── base_request.py      # Базовый класс для HTTP запросов
│   ├── user_api.py          # API для работы с пользователями + Pydantic модели
│   └── store_api.py         # API для работы с заказами + Pydantic модели
├── tests/
│   ├── __init__.py
│   ├── test_user_api.py     # Тесты для User API с Allure
│   └── test_store_api.py    # Тесты для Store API с Allure
├── conftest.py              # Фикстуры для тестов
├── requirements.txt         # Зависимости проекта
└── README.md               # Документация
```

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Установка Allure (если еще не установлен)

**Windows (через Scoop):**
```bash
scoop install allure
```

**Windows (через Chocolatey):**
```bash
choco install allure
```

**macOS:**
```bash
brew install allure
```

**Linux:**
```bash
sudo apt-add-repository ppa:qameta/allure
sudo apt-get update
sudo apt-get install allure
```

### 3. Запуск тестов

**Запуск всех тестов с генерацией Allure результатов:**
```bash
pytest --alluredir=allure-results -v
```

**Запуск конкретного файла тестов:**
```bash
pytest tests/test_user_api.py --alluredir=allure-results -v
```

**Запуск с маркерами:**
```bash
pytest -m critical --alluredir=allure-results -v
```

### 4. Генерация Allure отчета

**Генерация обычного отчета:**
```bash
allure generate allure-results -o allure-report --clean
allure open allure-report
```

**Генерация single-file HTML отчета:**
```bash
allure generate -c --single-file allure-results
```

Отчет будет находиться в папке `allure-report/index.html`

## 📊 Реализованные тесты

### User API Tests

1. ✅ **test_create_user** - Создание нового пользователя
   - Использует Pydantic модель `UserCreate` для валидации
   - Проверяет успешное создание пользователя

2. ✅ **test_get_user_by_username** - Получение пользователя по имени
   - Валидация ответа через Pydantic модель `User`
   - Проверка корректности данных

3. ✅ **test_update_user** - Обновление данных пользователя
   - Использует `UserUpdate` модель
   - Проверка обновления данных

4. ✅ **test_delete_user** - Удаление пользователя
   - Проверка успешного удаления

5. ✅ **test_create_user_invalid_email** - Негативный тест
   - Проверка валидации Pydantic при некорректном email

### Store API Tests

1. ✅ **test_get_inventory** - Получение инвентаря магазина
   - Проверка получения статистики по питомцам

2. ✅ **test_create_order** - Создание заказа
   - Использует `OrderCreate` модель для автогенерации JSON
   - Валидация ответа через `Order` модель

3. ✅ **test_get_order_by_id** - Получение заказа по ID
   - Проверка корректности данных заказа

4. ✅ **test_delete_order** - Удаление заказа
   - Проверка успешного удаления

## 🎯 Особенности реализации

### Pydantic модели

**Автогенерация JSON для запросов:**
```python
user_data = UserCreate(
    username="testuser",
    firstName="Test",
    lastName="User",
    email="test@example.com",
    password="password123"
)

# Конвертация в словарь для API запроса
api.create_user(user_data.to_dict())
```

**Валидация ответов от API:**
```python
response = api.get_user_by_username("testuser")
user = User(**response)  # Автоматическая валидация
assert user.username == "testuser"
```

### Allure отчеты

**Структурирование тестов:**
```python
@allure.feature('User API')
@allure.story('User Management')
@allure.title('Создание нового пользователя')
@allure.severity(allure.severity_level.CRITICAL)
def test_create_user(user_api):
    with allure.step('Создание данных пользователя'):
        # код теста
```

**Прикрепление данных к отчету:**
```python
allure.attach(
    str(response),
    name="API Response",
    attachment_type=allure.attachment_type.JSON
)
```

### Фикстуры

**Автоматическое создание и удаление тестовых данных:**
```python
@pytest.fixture(scope="function")
def create_test_user(user_api):
    # Создание пользователя перед тестом
    username = generate_random_username()
    user_api.create_user(user_data)
    
    yield username
    
    # Автоматическое удаление после теста
    user_api.delete_user(username)
```

## 🔧 Конфигурация

### pytest.ini (опционально)

Можно создать файл `pytest.ini` для дополнительной конфигурации:

```ini
[pytest]
addopts = -v --alluredir=allure-results
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: Smoke tests
    regression: Regression tests
    critical: Critical tests
```

## 📈 Пример отчета Allure

Allure отчет включает:
- 📊 **Overview** - общая статистика тестов
- 📋 **Suites** - группировка по тестовым наборам
- 📈 **Graphs** - графики и диаграммы
- ⏱️ **Timeline** - временная шкала выполнения
- 🔍 **Behaviors** - группировка по features и stories
- 📦 **Packages** - структура пакетов

## 🛠️ Технологии

- **Python 3.8+**
- **Pytest 7.4.3** - фреймворк тестирования
- **Allure 2.13.2** - отчеты
- **Pydantic 2.5.0** - валидация данных
- **Requests 2.31.0** - HTTP клиент

## 📝 Примечания

- Все тесты используют реальный API PetStore (https://petstore.swagger.io/v2)
- Тестовые данные создаются и удаляются автоматически
- Используются случайные имена пользователей для избежания конфликтов
- Все запросы и ответы логируются в консоль и Allure отчет

## 👨‍💻 Автор

Игорь - КТ5 Тестирование API

## 📄 Лицензия

Для образовательных целей
